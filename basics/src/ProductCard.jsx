import React from 'react'

const ProductCard = ({props}) => {
  return (
    <div>
        <h1>props.name</h1>
        <p>props.price</p>
        <p>props.category</p>
        (props.inStock ? )
    </div>
  )
}

export default ProductCard